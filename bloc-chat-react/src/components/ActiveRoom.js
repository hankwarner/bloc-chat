import React, { Component } from 'react';
import * as firebase from 'firebase';

class ActiveRoom extends React {
    constructor (props) {
        super(props);
    }

    render() {
        return(

        );
    }
}

export default ActiveRoom;